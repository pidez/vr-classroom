using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIMessageRoomJoined : UIMessageItem
{
    /*
    private string player;

    public UIMessageRoomJoined(string player) {
        if(string.IsNullOrEmpty(player))
            this.player = "unknown";
        this.player = player;
    }

    public override void SetLine() {
        this.messageLine.text = player + "joined the room";
    }
    */
}
